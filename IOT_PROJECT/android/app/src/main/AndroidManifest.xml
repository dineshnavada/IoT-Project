<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sit.myapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/car"
        android:label="Smart Parking"
        android:roundIcon="@mipmap/car"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Sit_And_Ssit" />
        <activity android:name=".ssit" />
        <activity android:name=".dynamic" />
        <activity android:name=".list" />
        <activity android:name=".username" />
        <activity android:name=".design" />
        <activity android:name=".user" />
        <activity android:name=".place" />
        <activity android:name=".aaaaaa"></activity>
    </application>

</manifest>